---
- hosts: localhost
  vars:
    user: ddragan
    become: true
    become_user: "{{ user }}"

  tasks:
    - debug:
        msg: "{{ local_user_home }}"

    - block:
      - debug:
          msg: "{{ local_user_home }}"

      - name: test become root
        apt:
          name: fzf
          state: present

        become: true
        become_user: root
